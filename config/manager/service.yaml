apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: controller-manager
  name: fintech-operator-controller-manager-metrics-service
spec:
  ports:
  - name: metrics
    port: 8443
    protocol: TCP
    targetPort: 8443
  selector:
    control-plane: controller-manager
