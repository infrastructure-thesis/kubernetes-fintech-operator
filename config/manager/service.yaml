apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: controller-manager
  name: fintech-operator-controller-manager-metrics-service
  namespace: fintech-operator-system
spec:
  ports:
  - name: metrics
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    control-plane: controller-manager
