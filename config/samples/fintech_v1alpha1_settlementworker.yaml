apiVersion: fintech.io/v1alpha1
kind: SettlementWorker
metadata:
  name: settlementworker-sample
spec:
  replicas: 3
  image: example.com/settlement-worker:v1.0
  settlementType: "ACH"
  schedulingPriority: "high"
  maxInFlightTransactions: 10000
  gracefulShutdownTimout: 30
